server:
  port: 8085
spring:
  application:
    name: jeecg-merck
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        shared-dataids: jeecg-cloud-application-beta.yml
feign:
  sentinel:
    enabled: true