<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.tenant.mapper.TenantMapper">

    <select id="getRoleIdByUserId" resultType="java.lang.String">
        SELECT
        sys_user_role.role_id
        FROM
        sys_user_role
        JOIN sys_role ON sys_role.id = sys_user_role.role_id
        <where>
            sys_user_role.user_id = #{userId} and sys_user_role.tenant_id = #{tenantId}
        </where>
    </select>
</mapper>