<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.business.mapper.ShoumanOrderMapper">
    <select id="findShoumanOrderToSend" resultType="org.jeecg.modules.business.entity.Shouman.ShoumanOrder">
        SELECT *
        FROM shouman_order
        WHERE success = 0;
    </select>

    <select id="findShoumanOrderWithStatusLessThan6" resultType="org.jeecg.modules.business.entity.Shouman.ShoumanOrder">
        SELECT *
        FROM shouman_order
        WHERE success = 1
          AND (status IS NULL OR status &lt; 6);
    </select>
</mapper>
