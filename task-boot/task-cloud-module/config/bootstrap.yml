server:
  port: 8080
spring:
  application:
    name: task-system
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        shared-dataids: task-cloud-application-beta.yml
feign:
  sentinel:
    enabled: true
