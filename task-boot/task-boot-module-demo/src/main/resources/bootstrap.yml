server:
  port: 7002
spring:
  profiles:
    active: dev
  application:
    name: task-demo
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
feign:
  sentinel:
    enabled: true
